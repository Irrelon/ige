import type { IgeTexture } from "../core/IgeTexture";
import type { IgeSmartFilter } from "@/types/IgeSmartFilter";

export declare const thresholdHelper: (imageData: ImageData, texture: IgeTexture, data: any) => ImageData;
export declare const threshold: IgeSmartFilter;
